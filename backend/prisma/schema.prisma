generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Game {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  puzzle    String
  solution  String
  difficulty Int
  category  String
}